import React from 'react';
import { useNavigate } from 'react-router-dom';

function ManagerProfile(){
    const navigate = useNavigate();
    return(
        <h1>ManagerProfile</h1>
    );
}
export default ManagerProfile